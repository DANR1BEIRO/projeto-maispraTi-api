spring:
  profiles:
   active: dev

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  config:
    import: optional:file:./secrets.properties

  server:
    port: ${PORT:8080}
